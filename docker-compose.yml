version: '3'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
      - redis
  db:
    image: postgres:11.5-alpine
    ports:
      - "5432:5432"
    env_file:
      - .env
  redis:
    image: redis:5.0-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD}

    ports:
      - "6379:6379"
    env_file:
      - .env